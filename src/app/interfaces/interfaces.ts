export interface Project {
    title: string;
    description: string;
    img: string;
    _id: string;
    createdAt: string;
    updatedAt: string;
}


export interface Profile{
    profiles: never[];
    profile: string;
    _id: string;
}
